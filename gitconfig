[core]
        pager = delta --diff-so-fancy
        excludesFile = ~/.gitignore

[pull]
	rebase = true

[push]
        autoSetupRemote = true

[diff]
        colorMoved = default

[merge]
        conflictstyle = diff3

[delta]
        navigate = true
        line-numbers = true
        side-by-side = true

[interactive]
        diffFilter = delta --color-only
