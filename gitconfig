[core]
        pager = delta
        excludesFile = ~/.gitignore

[pull]
	rebase = false

[push]
        autoSetupRemote = true

[diff]
        colorMoved = default

[merge]
        conflictstyle = diff3

[delta]
        navigate = true
        line-numbers = true

[interactive]
        diffFilter = delta --color-only
